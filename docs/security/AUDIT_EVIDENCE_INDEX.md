# Audit Evidence Index

Owner: ISMS Manager  
Review cadence: Monthly during refactor, quarterly after stabilization  
Status: Structured baseline complete; operational evidence in progress

Use this file as an index to evidence. Do not commit secrets, backups, customer exports, or sensitive logs.

## Operational Record Locations

- Access reviews: `docs/security/ACCESS_REVIEW_LOG.md`
- Restore tests: `docs/security/RESTORE_TEST_LOG.md`
- Internal audits: `docs/security/INTERNAL_AUDIT_LOG.md`
- Management reviews: `docs/security/MANAGEMENT_REVIEW_LOG.md`
- Feature-flag rollouts: `docs/security/FEATURE_FLAG_ROLLOUT_LOG.md`
- Incidents and drills: `docs/security/INCIDENT_POSTMORTEM_LOG.md`
- Vulnerability reviews: `docs/security/VULNERABILITY_REVIEW_LOG.md`
- Supplier reviews: `docs/security/SUPPLIER_REVIEW_LOG.md`
- Security awareness: `docs/security/SECURITY_AWARENESS_LOG.md`
- Segregation-of-duties baseline: `docs/security/SEGREGATION_OF_DUTIES.md`
- Governance contacts/threat baseline: `docs/security/GOVERNANCE_CONTACTS_AND_THREAT_INTELLIGENCE.md`
- Asset inventory baseline: `docs/security/ASSET_INVENTORY.md`
- Asset lifecycle log: `docs/security/ASSET_LIFECYCLE_LOG.md`
- Security readiness dashboard: `docs/security/READINESS_STATUS.md`
- Operational closeout checklist: `docs/security/OPERATIONAL_CLOSEOUT_STATUS.md`
- Operational record entry standard: `docs/security/OPERATIONAL_RECORD_ENTRY_STANDARD.md`
- ISMS operating rhythm: `docs/security/ISMS_OPERATING_RHYTHM.md`
- Release evidence checklist: `docs/security/RELEASE_EVIDENCE_CHECKLIST.md`
- Quarterly evidence packet: `docs/security/QUARTERLY_EVIDENCE_PACKET.md`
- Annual governance packet: `docs/security/ANNUAL_GOVERNANCE_PACKET.md`
- Information handling baseline: `docs/security/INFORMATION_HANDLING_STANDARD.md`
- Information transfer baseline: `docs/security/INFORMATION_TRANSFER_STANDARD.md`
- People/employment baseline: `docs/security/PEOPLE_AND_EMPLOYMENT_SECURITY_STANDARD.md`
- Offboarding and asset-return baseline: `docs/security/OFFBOARDING_AND_ASSET_RETURN_STANDARD.md`
- Physical/hosting baseline: `docs/security/PHYSICAL_AND_HOSTING_SECURITY_STANDARD.md`
- Asset disposal/reuse baseline: `docs/security/ASSET_DISPOSAL_AND_REUSE_STANDARD.md`
- Environment separation baseline: `docs/security/ENVIRONMENT_SEPARATION_STANDARD.md`
- Test information baseline: `docs/security/TEST_INFORMATION_STANDARD.md`
- Supplier assurance baseline: `docs/security/SUPPLIER_ASSURANCE_STANDARD.md`
- Business continuity baseline: `docs/security/BUSINESS_CONTINUITY_STANDARD.md`
- Hosting resilience baseline: `docs/security/HOSTING_RESILIENCE_STANDARD.md`
- Legal, records, and privacy baseline: `docs/security/LEGAL_RECORDS_AND_PRIVACY_STANDARD.md`
- Remote working baseline: `docs/security/REMOTE_WORKING_STANDARD.md`
- Authentication and cryptography baseline: `docs/security/AUTHENTICATION_AND_CRYPTOGRAPHY_STANDARD.md`
- Configuration and application-security baseline: `docs/security/CONFIGURATION_AND_APPLICATION_SECURITY_STANDARD.md`
- Endpoint/operations baseline: `docs/security/ENDPOINT_AND_OPERATIONS_SECURITY_STANDARD.md`
- Data-protection lifecycle baseline: `docs/security/DATA_PROTECTION_LIFECYCLE_STANDARD.md`
- Network/service baseline: `docs/security/NETWORK_AND_SERVICE_SECURITY_STANDARD.md`
- Web-filtering baseline: `docs/security/WEB_FILTERING_BASELINE.md`
- Access review workflow: `docs/security/ACCESS_REVIEW_CHECKLIST.md`
- Restore-test workflow: `docs/security/RESTORE_TEST_PLAYBOOK.md`
- Incident drill workflow: `docs/security/INCIDENT_RESPONSE_DRILL_PLAYBOOK.md`
- Vulnerability review workflow: `docs/security/VULNERABILITY_REVIEW_PLAYBOOK.md`
- Internal audit workflow: `docs/security/INTERNAL_AUDIT_PLAYBOOK.md`
- Management review workflow: `docs/security/MANAGEMENT_REVIEW_AGENDA.md`
- Detailed Annex A tracker: `docs/security/ANNEX_A_CONTROL_TRACKER.md`
- Supplier review workflow: `docs/security/SUPPLIER_REVIEW_PLAYBOOK.md`
- Security awareness workflow: `docs/security/SECURITY_AWARENESS_PLAYBOOK.md`

## Change Evidence

| Date       | Change                                                      | PR/commit                                                  | Evidence                                                                                                                                                                                                                                                                             | Owner            |
| ---------- | ----------------------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| 2026-04-19 | AI-native refactor and ISO readiness foundation             | PR #432 / `1cbcc31`                                        | Baseline docs, AI rules, production-data safety guidance, refactor playbook, and initial quality-gate scripts added as engineering evidence                                                                                                                                          | Engineering lead |
| 2026-04-19 | Feature flag foundation                                     | PR #437 / `3a1b5c0`                                        | `ff_feature_flag_audit_events`, `aud_audit_logs`, `/api/v1/feature-flags` handlers, rollout evaluation, and API contract coverage                                                                                                                                                    | Engineering lead |
| 2026-04-21 | Auth route boundary cleanup                                 | PR #489 / `3ced973`                                        | Profile/change-password handlers moved behind auth module; route files remain thin wrappers; no schema/data changes                                                                                                                                                                  | Engineering lead |
| 2026-04-21 | Staff API route boundary cleanup                            | PR #490 / `920f905`                                        | Staff handlers moved behind auth module; legacy and v1 route files are thin wrappers; no schema/data changes                                                                                                                                                                         | Engineering lead |
| 2026-04-21 | Dev-login route boundary cleanup                            | PR #491 / `3b7425d`                                        | Dev-only login handler moved behind auth module; route files remain thin wrappers; no schema/data changes                                                                                                                                                                            | Engineering lead |
| 2026-04-21 | Hotel demo seed route boundary cleanup                      | PR #492 / `febb34e`                                        | Demo seed handler moved behind hotel demo module; route file remains a thin wrapper; no schema/data changes                                                                                                                                                                          | Engineering lead |
| 2026-04-21 | Health route boundary cleanup                               | PR #493 / `d339fca`                                        | Health readiness checks moved behind platform health layer; route file remains a thin wrapper; no schema/data changes                                                                                                                                                                | Engineering lead |
| 2026-04-23 | Quality gate hardening and browser API boundary enforcement | PRs #542-#544 / `78f4531`, `9b25ad5`, `dea51f3`            | Added text-encoding guard, enforced dependency-boundary validation in PR CI, moved offer template preview onto the shared templates API client, and added BOM regression coverage                                                                                                    | Engineering lead |
| 2026-04-23 | Reports boundary cleanup                                    | PR #546 / `db30c64`                                        | Reports page now loads customers, leads, offers, projects, meetings, and announcements through shared feature API loaders instead of inline endpoint descriptors                                                                                                                     | Engineering lead |
| 2026-04-23 | Branding and theme precedence convergence                   | PRs #547-#549 / `aa943c7`, `d91c55c`, `cc8fbe8`            | Shared branding resolver introduced, organization theme fallbacks added, theme-settings handlers kept behind thin routes, and precedence covered by branding/theme bootstrap tests                                                                                                   | Engineering lead |
| 2026-04-23 | Offer browser API boundary cleanup                          | PRs #550-#551 / `1309c31`, `124fc23`                       | Offer wizard lifecycle/lookups/submit flows and dashboard PDF actions now use shared `/api/v1` clients/helpers instead of direct browser fetches or legacy inline paths                                                                                                              | Engineering lead |
| 2026-04-23 | Governance evidence baseline refresh                        | PR #552 / `e15c560`                                        | Audit evidence, supplier register, and risk register updated so already-completed refactor work has traceable repo-backed references instead of placeholder-only `TBD` rows                                                                                                          | Engineering lead |
| 2026-04-23 | Branding API client and cleanup inventory refresh           | PR #553 / `0365c7c`                                        | Dedicated `branding.api.ts` client added for theme sync, appearance flows use the shared client surface, and the generated cleanup inventory reflects the active feature API client set                                                                                              | Engineering lead |
| 2026-04-23 | PR quality gate expansion                                   | PRs #554-#555 / `2c9f5d6`, `b8171b7`                       | Pull-request CI now enforces lint, tests, typecheck, build, migration safety, AI proxy consistency, dependency boundaries, encoding, and file-size checks with explicit Prisma client generation in CI                                                                               | Engineering lead |
| 2026-04-23 | Legacy API inventory refinement                             | PRs #556-#557 / `8a86836`, `cbcd10e`                       | Cleanup inventory distinguishes compatibility wrappers from retained non-versioned routes, tracks literal legacy `/api/*` references, and reuses cache state more effectively in CI                                                                                                  | Engineering lead |
| 2026-04-24 | Reproducible VPS deploy workflow                            | PRs #558-#561 / `0397b5f`, `9614340`, `c61224b`, `90e6a12` | Deploys build release artifacts in GitHub, sync the tracked deploy script onto the VPS, use the fixed deploy path under `/var/www/offert`, keep build artifacts in `.deploy-state`, and stop rerunning the full PR quality pipeline after merge                                      | Engineering lead |
| 2026-04-24 | Security evidence merge gate                                | PR #564 / `6ff3e61`                                        | Pull-request CI now fails security-relevant workflow, migration, auth, feature-flag, and public-offer/signing changes unless the PR also updates repo-backed security/evidence docs                                                                                                  | Engineering lead |
| 2026-04-24 | Deploy artifact promotion fallback hardening                | PR #565 / `8376328`                                        | Deploy promotion now treats GitHub CLI, artifact lookup, download, unzip, tar, and metadata parsing failures as safe fallback conditions, so production deploys rebuild in CI instead of aborting before the VPS release path runs                                                   | Engineering lead |
| 2026-04-24 | Shared client migration continuation and wrapper retirement | PRs #566-#569 / `8c26388`, `900d6bb`, `05b7d1b`, `2f8120d` | Hotel setup and calendar UI flows use shared `/api/v1` clients, and the legacy calendar, announcements, and staff wrappers were retired after repo usage verification and cleanup inventory regeneration; no schema or production-data changes                                       | Engineering lead |
| 2026-04-24 | Feature-flag wrapper retirement                             | PR #579 / `d34e7df`                                        | Legacy `/api/feature-flags` compatibility wrappers retired after repo usage verification; `/api/v1/feature-flags` remains the active contract surface and browser client target; no schema or production-data changes                                                                | Engineering lead |
| 2026-04-24 | Admin compliance wrapper retirement                         | PR #581 / `bf6266b`                                        | Added the missing `/api/v1/admin/compliance/controls/[id]/evidence` route, retired legacy `/api/admin/compliance` and `/api/admin/access-review` wrappers after repo usage verification, and refreshed the cleanup inventory; no schema or production-data changes                   | Engineering lead |
| 2026-04-24 | Auth wrapper retirement                                     | PR #582 / `4ec6b8b`                                        | Added the missing `/api/v1/auth` MFA backup-code and WebAuthn registration routes, made `v1` auth routes self-contained, and retired legacy `/api/auth` compatibility wrappers after repo usage verification; no schema or production-data changes                                   | Engineering lead |
| 2026-04-24 | Security operations evidence logs                           | PR #585 / `6cc847f`                                        | Added repo-backed operational log templates for access reviews, restore tests, internal audits, management reviews, incidents, and vulnerability triage; linked those logs from the security policies, audit evidence index, and SoA without creating fake records                   | Engineering lead |
| 2026-04-24 | Feature-flag rollout evidence log                           | PR #586 / `75d17e1`                                        | Added a repo-backed rollout log for production-impacting release flags and linked it from change management, the audit evidence index, and the SoA so rollout/rollback evidence can be recorded without inventing fake entries                                                       | Engineering lead |
| 2026-04-24 | Internal audit and management review playbooks              | PR #587 / `1e2d343`                                        | Added repo-backed internal audit and management review workflows, linked them from the corresponding logs, and tightened the evidence index so those two remaining operating gaps are executable without inventing records                                                           | Engineering lead |
| 2026-04-24 | Access review and restore test workflows                    | PR #589 / `0163ea5`                                        | Added repo-backed workflows for quarterly access reviews and non-production restore tests, linked them from the corresponding logs, and tightened the evidence index and SoA so those operating gaps are executable without inventing records                                        | Engineering lead |
| 2026-04-24 | Incident and vulnerability workflows                        | PR #590 / `1b54dd3`                                        | Added repo-backed workflows for incident-response drills and vulnerability reviews, linked them from the corresponding logs and policies, and corrected the evidence index so the merged access/restore work points at PR #589                                                       | Engineering lead |
| 2026-04-24 | Supplier review workflows                                   | PR #593 / `c0b694a`                                        | Added repo-backed supplier review log and playbook, linked them from supplier management and the evidence index, and made the supplier-relationship controls more actionable without inventing review records                                                                        | Engineering lead |
| 2026-04-24 | Annex A control tracker                                     | PR #592 / `cbb51a4`                                        | Added a repo-backed tracker for all 93 ISO/IEC 27001:2022 Annex A controls, upgraded the SoA to a structured baseline that points at that tracker, linked the tracker from the evidence index, and fixed the mojibake risk-score formula                                             | Engineering lead |
| 2026-04-24 | Security awareness workflows                                | PR #594 / `98d1806`                                        | Added repo-backed security awareness log and playbook, linked awareness tracking into AI usage policy and the evidence index, and made the awareness/training control more actionable without inventing completion records                                                           | Engineering lead |
| 2026-04-25 | Deploy scope classification tightening                      | PR #605 / `f30549b`                                        | Narrowed deploy-on-main classification so production deploys and CI rebuilds only run for runtime-affecting changes or real release-path updates such as `deploy.yml` and `scripts/deploy-release.sh`, while non-release check/policy scripts still stay covered by PR quality gates | Engineering lead |
| 2026-04-25 | Refactor-plan status dashboard and guard                    | PR #607 / `f1db9b3`                                        | Added a generated repo-backed plan status snapshot, wired it into PR CI, and linked the refactor plan to a current status document so remaining structural versus operational work stays explicit without hand-maintained drift                                                      | Engineering lead |
| 2026-04-25 | Legacy API literal regression guard                         | PR #607 / `c89ce9c`                                        | Added a CI guard that blocks new unapproved non-versioned `/api/*` literals outside route files, documented the allowed exception classes in API versioning, and tied the rule into change-management evidence so the `/api/v1` migration cannot silently regress                    | Engineering lead |
| 2026-04-25 | Operational evidence coverage guard                         | PR #609 / `c132f33`                                        | Added a CI guard that validates empty operational evidence registers, open-gap rows in `AUDIT_EVIDENCE_INDEX.md`, and owner assignments stay aligned so readiness tracking cannot silently drift away from the actual repo-backed operating logs                                     | Engineering lead |
| 2026-04-25 | Shared operational evidence registry                        | PR #610 / `89d3b23`                                        | Consolidated operational evidence ownership, review cadence, and next-action metadata into a shared registry used by the readiness dashboard and operational-evidence guard, and tightened CI classification so registry-only changes stay on the process-validation path            | Engineering lead |
| 2026-04-25 | Lean operational cadence summary                            | PR #612 / `c8cb96c`                                        | Added a generated minimum operating-cadence section to the operational closeout dashboard so the remaining stage-2 work is easier to run as a small, recurring operating rhythm instead of a pile of disconnected checklists                                                         | Engineering lead |
| 2026-04-25 | Auth profile theme fallback typecheck fix                   | PR #613 / `fb8d637`                                        | Moved organization theme fallback assembly out of the auth repository and into the auth account service via the identity module, restoring a clean typecheck baseline for auth profile theming without schema or production-data changes                                             | Engineering lead |
| 2026-04-25 | ISMS operations packet                                      | PR #614 / `d755b1d`                                        | Added a lightweight operating-rhythm doc plus release, quarterly, and annual execution packets so the remaining stage-2 work can be run in larger, practical batches without inventing evidence or building more governance machinery                                                | Engineering lead |
| 2026-05-15 | MFA v1 factor-state hardening and helpdesk recovery rollout | Local working tree                                         | Added pending TOTP enrollment state, passkey-aware MFA truth calculation, self-service factor management guardrails, dedicated Account Security UI, org-scoped helpdesk MFA recovery with audit logging, and supporting `/api/v1/auth` recovery/session endpoints                 | Engineering lead |
| 2026-05-15 | Deploy hotfix and dependency security cleanup               | Local working tree                                         | Removed production deploy's dependency on GitHub artifact uploads by shipping the release bundle directly from the release job to the VPS, restored JWT revocation enforcement for passkey deletion, preserved cookie-clearing raw responses through `createHandler`, and reduced `npm audit` to moderate-only findings with targeted dependency bumps and overrides | Engineering lead |
| 2026-05-15 | Manual main redeploy trigger                               | Local working tree                                         | Added a `workflow_dispatch` path to the VPS deploy workflow so operators can redeploy the latest `main` release with the current artifact-free flow instead of rerunning an older historical push workflow that may still depend on retired artifact-upload steps, and restricted that manual path to the `refs/heads/main` branch ref so non-main branches or tags cannot be deployed to production through the redeploy shortcut | Engineering lead |
| 2026-05-15 | MFA replay resistance, passkey metadata, and MFA surface redesign | Local working tree                                    | Added TOTP replay blocking, atomic backup-code consumption, factor-mutation audit events, additive passkey credential metadata, explicit WebAuthn production configuration, passkey rename support, and refreshed end-user/helpdesk MFA interfaces with shared reusable UI primitives | Engineering lead |
| 2026-05-16 | API route lifecycle guard and duplicate-surface evidence         | Local working tree                                    | Added route-family lifecycle auditing, explicit temporary-overlap metadata, CI enforcement against unregistered or expired duplicate legacy/V1 product routes, inventory visibility for canonical/exception/overlap/duplicate states, and policy docs clarifying that feature flags do not justify indefinite duplicate external API paths | Engineering lead |
| 2026-05-16 | API overlap expiry validation hardening                          | Local working tree                                    | Tightened temporary API-overlap metadata validation so impossible calendar dates such as `2026-02-31` are rejected instead of being normalized by JavaScript date parsing, preserving the intended expiry enforcement of the route-lifecycle guard | Engineering lead |
| 2026-05-28 | Soleria login redesign and self-registration shutdown            | Local working tree                                    | Replaced `/logga-in` with a new Soleria-specific internal workspace UI, preserved existing email/password/MFA auth behavior, changed `/registrera` to an intentional 404, kept the backend registration endpoint disabled, and locally validated with `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, and `npm run check:security-evidence` | Engineering lead |
| 2026-05-31 | Dashboard auth/rate-limit resilience and MFA grace extension     | Pending PR                                           | Extended access-token cookies to two hours, scoped authenticated API limits by user and route with a more generous safeguard budget, refreshed the dashboard automatically, and extended MFA enrollment grace while MFA remains in development | Engineering lead |
| 2026-06-03 | RBAC enforcement, role seeding, auth bug fixes, and company scoping | PR #653 / `e0671e1`, `6012e61`, `71af725`, `ec8e97d` | Wired RBAC permission check in `createHandler` middleware (previously a no-op); seeded all 7 system roles and ERP permissions via idempotent migration; added missing offers/products/companies/projects/procurement permissions to admin/user/viewer roles; fixed `email: payload.sub` UUID bug in server-session service; guarded `super_admin` assignment so it can only be done via VPS seed scripts; added `users.read/write/delete` and `companies.read/write/delete` permission declarations to staff and company handlers; moved `enforceRateLimit` before RBAC check so low-privilege users consume budget on 403 responses; invalidated permission cache after role assignment; required `companyId` on all offer/template/product/category creates with org ownership validation | Engineering lead |
| 2026-06-04 | ERP schema foundation — invoicing, time tracking, custom fields, shared catalog | `claude/erp-foundation` | Additive schema migration only: new tables `inv_invoices`, `inv_line_items`, `prj_time_entries`, `org_custom_field_definitions`; new nullable columns `customFields JSONB` on offers/products/companies/leads/customers/projects; `sharedAcrossOrg` flag on products; no existing data touched; Prisma client regenerated; typecheck clean | Engineering lead |
| 2026-06-04 | Company localisation & tax layer — currency, VAT number, default VAT rate per brand | `claude/erp-foundation` | Additive migration `20260604000002`: three columns on `off_companies` (`currency TEXT DEFAULT 'SEK'`, `vatNumber TEXT`, `defaultVatRate FLOAT DEFAULT 0.25`); enables multi-currency multi-VAT-regime operation; no existing rows modified | Engineering lead |
| 2026-06-04 | Lead intake inbound webhook and CRM linking                     | Branch `codex/lead-intake-forwarder`                 | Added Resend inbound webhook verification, company-scoped lead intake forwarder configuration, JSONB field mappings, non-converting lead/customer linking, recipient suggestion APIs, canonical `/api/v1/webhooks/resend/inbound` plus registered temporary non-versioned overlap, rebased after ERP foundation schema changes, and locally validated with `npx prisma generate`, `npm run typecheck`, `npm test`, `npm run check:migrations`, `npm run check:api-route-lifecycle`, `npm run check:file-size`, `npm run check:encoding`, and `npm run check:security-evidence` | Engineering lead |
| 2026-06-04 | Dependency vulnerability remediation (`npm audit fix`) shipped with ERP foundation deploy | `claude/erp-foundation-m0` | Ran non-breaking `npm audit fix`: bumped `vitest`→4.1.8 (clears critical Vitest UI arbitrary-file read/exec advisory; dev-only, not shipped to prod), patched the `qs` DoS and the `uuid`→`svix`→`resend` chain; reduced findings from 9 (1 critical, 8 moderate) to 3 moderate. Remaining `postcss`-via-`next`/`@scalar` advisories left unpatched because the only offered fix downgrades Next.js to 9.3.3 (breaking) — accepted as low real-world risk (build-time CSS stringify, requires attacker-controlled stylesheet). Locally validated: lint, typecheck, 182 tests, and production build all green | Engineering lead |
| 2026-06-04 | CI/CD pipeline performance + hardening (no app/schema/data changes) | `claude/ci-speedups` | Quality-gates + VPS deploy workflow tuning: cache `node_modules` keyed on `package-lock`+`package.json` hash (restore + success-gated save) so redundant `npm ci` is skipped on hit; decoupled the `.next/cache` key from dependency/schema hashes so incremental builds stay warm across dep/schema PRs; excluded `.next/cache` from the shipped build artifact; raised PR release-bundle retention 1→7 days to keep artifact promotion (avoids prod rebuild); removed now-default `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`; pinned `appleboy/scp-action@v1.0.0`; silenced npm update-notifier/fund notices in deploy logs. Validated with `actionlint`, `bash -n`, and full lint/typecheck/test/build | Engineering lead |
| 2026-06-05 | Custom Fields module (M1) backend — definitions API and RBAC permissions | Branch `claude/m1-custom-fields` | Added DDD `custom-fields` module (domain types + pure `slugifyKey`/`validateCustomFieldValues`, Prisma repository, application service, JWT API handlers) over the already-deployed `org_custom_field_definitions` table; org-scoped soft-delete CRUD with select-option/duplicate-key/union validation; thin `/api/v1/custom-fields` and `/api/v1/custom-fields/[id]` routes guarded by new `custom_fields.read`/`custom_fields.write` permissions; no Prisma schema or production-data changes; locally validated with `prisma generate`, `tsc --noEmit`, `check-migrations`, `check-security-evidence`, `check-plan-status`, `check-api-route-lifecycle`, `check-file-size`, `check-encoding`, and `npm run lint` | Engineering lead |
| 2026-06-06 | Invoicing schema foundation (M3) — credit notes, immutability, ROT/RUT | Branch `claude/m3-invoicing` | Additive migration `20260606000001_invoicing_compliance_rotrut` extends the (empty) `inv_invoices`/`inv_line_items` tables: `documentType` + self-ref `creditedInvoiceId` (credit notes share the gapless number series), `issuedAt`/`paymentReference` (immutability + payment ref), ROT/RUT claimant/property/labour/deduction columns, and a line-level `lineType`/`rotRutEligible` labour-material split. All columns nullable or defaulted; no existing data touched (tables empty). Validated: `prisma generate`, `tsc --noEmit`, `check-migrations` | Engineering lead |
| 2026-06-06 | Invoicing module (M3) core backend — draft CRUD, gapless-at-issue numbering, issue/send + mark-paid, create-from-offer/time | Branch `claude/m3-invoicing` | Added DDD `invoicing` module (domain status guards + pure pricing/totals, Prisma repository with org-scoped soft-delete CRUD, source-document repository, application services, JWT API handlers) over the already-deployed `inv_invoices`/`inv_line_items` tables. Numbers are assigned only at issue/send inside a Serializable transaction with retry on the `(organizationId, invoiceNumber)` unique constraint (mirrors offers' `assignOfferNumber`); drafts hold a `0` placeholder and issued invoices are never edited or deleted, keeping the series gapless. Create-from-offer copies recipient + line items; create-from-time groups billable, unbilled `TimeEntry` rows into labour lines and sets their `invoiceId` (billed) in the same flow. Thin `/api/v1/invoices`, `/[id]`, `/[id]/send`, `/[id]/mark-paid` routes guarded by new `invoices.read/write/send/delete` permissions; `invoice.sent`/`invoice.paid` domain events emitted. No Prisma schema or production-data changes; locally validated with `prisma generate`, `tsc --noEmit`, `check-migration-safety`, `check-security-evidence`, `check-plan-status`, `check-api-route-lifecycle`, `check-file-size`, `check-text-encoding`, `npm run lint`, and `npm run lint:deps` | Engineering lead |
| 2026-06-06 | Invoicing module (M3) Phase C — invoice PDF + email on send | Branch `claude/m3-invoicing` | Added self-contained invoice PDF generation and recipient email to the issue/send flow. New `invoice-pdf-format` (view-model shapes + sv-SE money/percent/quantity formatters + per-rate VAT breakdown), `invoice-pdf` (lazy-singleton headless Chromium A4 renderer + entity→model mapper, mirroring offers' Playwright pattern with no `@modules/supporting/offers` import), and `invoice-email` (minimal Resend send: no-op without `RESEND_API_KEY`, `RESEND_TEST_TO` redirect, onboarding-sender fallback). `sendInvoice` now, after the committed number/status transition, loads the selling company, renders the archival PDF once (freeze-once — never re-rendered if `generatedPdf` bytes already exist), stores it on the invoice, then emails the recipient a link to the PDF route. PDF and email are best-effort: both are wrapped so any failure is logged and swallowed and never blocks issuing or rolls back the number/status. Repository gained org-scoped `storeGeneratedPdf`/`getGeneratedPdf` (the `generatedPdf` column stays out of the shared selects). New JWT, `invoices.read`-gated, org-scoped `GET /api/v1/invoices/[id]/pdf` streams the stored bytes as `application/pdf` (inline, `Cache-Control: private, no-store`; 404 when not issued or no bytes). No Prisma schema or production-data changes. Validated: `prisma generate`, `tsc --noEmit` (clean), `npm run lint` (0 errors), `npm run lint:deps` (0 new violations — no invoicing→offers edge), production `npm run build`, `check-api-route-lifecycle`, `check-file-size`, `check-text-encoding`, `check-security-evidence` | Engineering lead |
| 2026-06-06 | Invoicing module (M3) Phase D — credit notes (kreditfaktura) | Branch `claude/m3-invoicing` | Added a flow to issue a credit note that fully reverses an issued invoice. New `canCredit` domain guard (only `documentType==='invoice'` in status `sent`/`viewed`/`paid`/`overdue` may be credited — drafts, cancelled, already-`credited`, and credit notes themselves are rejected). New `invoice-credit.service.ts#createCreditNote(orgId, actorId, originalInvoiceId, { reason? })` loads the org-scoped original (→404 when missing, `Errors.conflict`→409 when not creditable), creates a draft `documentType='credit_note'` with `creditedInvoiceId=original.id`, copies recipient/company/currency/payment reference, sets issueDate=dueDate=today, and reverses every line by negating quantity (`quantity: -item.quantity`, unitPrice/vatRate/discount/lineType/rotRutEligible preserved) so the pure totals helper yields negative ex-VAT/VAT/inc-VAT, reducing the customer's balance; notes carry `Kreditfaktura avseende faktura {invoiceNumber}` plus the optional reason. After the draft exists the original is moved to status `credited` via `applyTransition`, which (with the `canCredit` guard) prevents a second credit note on the same invoice. The caller issues the draft via the existing `sendInvoice` — credit notes share the gapless number series and the existing PDF/email render the "Kreditfaktura". `CreateInvoiceData`/`create` extended with optional `documentType` (default `'invoice'`) + `creditedInvoiceId` (default null) so existing callers are unaffected. New JWT, `invoices.write`-gated, org-scoped `POST /api/v1/invoices/[id]/credit` handler returns `created(creditNote, location)` (201) or 404/409. No Prisma schema or production-data changes. Validated: `prisma generate`, `tsc --noEmit` (clean), `npm run lint` (0 errors), `npm run lint:deps` (0 new violations — no invoicing→offers edge), production `npm run build`, `check-api-route-lifecycle`, `check-file-size`, `check-text-encoding`, `check-security-evidence` | Engineering lead |
| 2026-06-06 | Invoicing module (M3) Phase E — ROT/RUT deduction + HUS export | Branch `claude/m3-invoicing` | Wired the previously-built ROT/RUT domain + service + PDF into the API. Pure domain `rot-rut.ts` (`computeRotRut`/`validateRotRutBuyer`/`rotRutRate`/`requiresProperty`, 30% ROT / 50% RUT on the inc-VAT eligible-labour basis — `RotRutLineLike.lineType`/`rotRutEligible` are optional so it accepts both `InvoiceLineItem` and `InvoiceLineItemInput`; non-labour/ineligible lines are skipped) and pure XML-escaping `hus-xml.ts#buildHusXml(HusClaim)` (Skatteverket "Begäran om utbetalning – Husarbete" — best-effort structure, XSD must be validated before live filing; no public API). New `invoice-rotrut-export.service.ts#buildRotRutExport(id, orgId)` loads the org-scoped invoice (null when missing or no `rotRutType`), resolves the seller via `getInvoiceCompany`, and builds the claim with `paidByBuyer = totalIncVat − rotRutDeductionAmount` and `requestedAmount = rotRutDeductionAmount`. New JWT handlers: `POST /api/v1/invoices/[id]/rotrut` (`invoices.write`, Zod `rotRutType: enum(['ROT','RUT']).nullable().optional()` + buyer fields; `setInvoiceRotRut` throws `Errors.conflict`→409 on issued / `Errors.badRequest`→400 on missing buyer info, null→404) and `GET /api/v1/invoices/[id]/rotrut-export` (`invoices.read`, org-scoped, streams `application/xml; charset=utf-8` attachment `husarbete-<number|id>.xml`, `Cache-Control: private, no-store`, 404 when no claim). `extractId` generalised to a known-action-segment set (`send`/`mark-paid`/`credit`/`pdf`/`rotrut`/`rotrut-export`). No Prisma schema or production-data changes; no `@modules/supporting/offers` import. Validated: `prisma generate`, `rm -rf .next` + `tsc --noEmit` (clean), `npm run lint` (0 errors), `npm run lint:deps` (0 new violations — no invoicing→offers edge), production `npm run build` (both routes registered), `check-api-route-lifecycle`, `check-file-size`, `check-text-encoding`, `check-security-evidence` | Engineering lead |
| 2026-06-08 | Invoicing module (M3) Phase F — invoice UI (fakturor) | Branch `claude/m3-invoicing` | Added the dashboard UI over the M3 invoicing API: `/fakturor` list (status tabs/badges, date range, pagination), `/fakturor/ny` (create a blank draft or create-from-accepted-offer), and `/fakturor/[id]` which branches draft editor (recipient/dates/payment-ref/notes/line items + ROT/RUT panel; save/send/delete) / credit-note review-and-issue / read-only issued view (mark-paid, create credit note, PDF download, Husarbete ROT/RUT XML export); plus a "Skapa faktura" action on accepted offers. Typed `invoices.api.ts` client and a Fakturor sidebar entry. App-layer status guards and pricing mirror the invoicing domain (boundary rules forbid importing module internals); the server re-validates every transition. Frontend-only — no Prisma schema, migration, or production-data changes. Validated: `tsc --noEmit`, `npm run lint`, `npm run lint:deps` (no new violations), production `npm run build`, `vitest run` (191 passed), `check-file-size`, `check-migration-safety`, `check-security-evidence`, `check-plan-status` | Engineering lead |
| 2026-06-05 | Time Tracking module (M2) backend — time-entry API and RBAC permissions | Branch `claude/m2-time-tracking` | Added time-entry domain types, org-scoped soft-delete Prisma repository, and application service to the `projects` module over the already-deployed `prj_time_entries` table; rules enforce `hours` in (0, 24], required `date`, `billable` default true, actor logs own time, and edit/delete allowed only for the owning user or an admin (`admin`/`super_admin`); JWT handlers guard new `time_entries.read`/`time_entries.write` permissions; thin `/api/v1/time-entries` and `/api/v1/time-entries/[id]` routes; no Prisma schema or production-data changes; locally validated with `prisma generate`, `tsc --noEmit`, `check-migrations`, `check-security-evidence`, `check-plan-status`, `check-api-route-lifecycle`, `check-file-size`, `check-encoding`, `npm run lint`, and `npm run lint:deps` | Engineering lead |

## Feature Flag Rollout Evidence

| Date                      | Flag                 | Environment | Rollout evidence                                                                                                                 | Rollback evidence                                                                                  | Owner            |
| ------------------------- | -------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ---------------- |
| Open gap as of 2026-04-24 | Public offer rewrite | production  | Repo-backed rollout log now exists in `FEATURE_FLAG_ROLLOUT_LOG.md`, but no completed production rollout record is committed yet | Roll back by disabling the renderer flag and falling back to `legacy` in the public offer renderer | Engineering lead |

## Migration Evidence

| Date       | Migration                               | Backup ref                                                                                                                              | Validation                                                                                                                                                                      | Owner            |
| ---------- | --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| 2026-04-23 | `20260423225200_add_org_theme_defaults` | Deployment backup evidence is external to the repo and still needs to be recorded in the operations log before future production replay | PR #548 / `d91c55c`; additive nullable columns only; `npm run check:migrations`; `tests/unit/theme-bootstrap.test.ts`; `prisma generate`; typecheck with a dummy `DATABASE_URL` | Engineering lead |
| 2026-05-15 | `20260515000001_add_pending_totp_helpdesk_recovery` | Apply with the standard deployment backup procedure before production rollout; migration is additive only and does not remove or rewrite production records | Local validation: `npx prisma generate`; `npm run check:migrations`; `npm run typecheck`; `npm test`; migration adds nullable `pendingTotpSecret`, idempotent RBAC rows for `helpdesk` and `users.mfa_reset`, and a safe `mfaEnabled` backfill based on enrolled factors | Engineering lead |
| 2026-05-15 | `20260515000002_add_webauthn_credential_metadata` | Apply with the standard deployment backup procedure before production rollout; migration is additive only and does not remove or rewrite production records | Local validation: `npx prisma generate`; migration adds nullable passkey metadata (`aaguid`, device type, backup state, transports) for richer account-security UX without changing existing credentials | Engineering lead |
| 2026-05-31 | `20260531000001_extend_mfa_grace_deadline` | Apply with the standard deployment backup procedure before production rollout; migration is a forward-only non-destructive data update requested for active MFA development | Pending validation: `npm run check:migrations`; updates non-deleted users without enrolled MFA so `mfaGraceExpiresAt` is at least two years out, preserving enrolled MFA state and deleting no data | Engineering lead |
| 2026-06-03 | `20260603000001_seed_all_system_roles_and_permissions` | Apply with the standard deployment backup procedure; migration is purely additive `INSERT ... ON CONFLICT DO NOTHING` — no DROP, TRUNCATE, or data rewrites | Local validation: `npm run check:migrations`; seeds all 7 system roles and ERP permissions into `usr_roles`, `usr_permissions`, `usr_role_permissions`; idempotent and safe to re-apply | Engineering lead |
| 2026-06-04 | `20260604000001_erp_foundation` | Apply with the standard deployment backup procedure before production rollout; migration is purely additive — no DROP, ALTER TYPE, or data rewrites | Local validation: `prisma validate`; `prisma generate`; `tsc --noEmit`; `check-security-evidence`; creates `inv_invoices`, `inv_line_items`, `prj_time_entries`, `org_custom_field_definitions`; adds nullable JSONB and boolean columns only | Engineering lead |
| 2026-06-04 | `20260604000002_company_localization` | Apply with the standard deployment backup procedure; purely additive three-column ALTER TABLE on `off_companies`; no data rewrites | Local validation: `prisma generate`; `tsc --noEmit`; `check-security-evidence` | Engineering lead |
| 2026-06-04 | `20260604000003_lead_intake_forwarder` | Apply with the standard deployment backup procedure before production rollout; migration is additive only and does not remove or rewrite production records | Local validation: `npx prisma generate`; `npm run check:migrations`; `npm run typecheck`; `npm test`; adds nullable lead/customer fields and new lead intake tables/indexes for company-scoped inbound processing | Engineering lead |
| 2026-06-05 | `20260605000001_custom_fields_permissions` | Apply with the standard deployment backup procedure; migration is purely additive `INSERT ... ON CONFLICT DO NOTHING` — no DROP, TRUNCATE, ALTER, or data rewrites, safe to deploy with zero downtime and safe to re-apply | Local validation (branch `claude/m1-custom-fields`): `npm run check:migrations`; seeds `custom_fields.read`/`custom_fields.write` into `usr_permissions` and grants them via `usr_role_permissions` (read to admin/user/viewer, write to admin only); no schema changes (data-only) | Engineering lead |
| 2026-06-06 | `20260606000001_invoicing_compliance_rotrut` | Apply with the standard deployment backup procedure; purely additive — new nullable/defaulted columns + a self-referential FK + two indexes on the (empty) `inv_invoices`/`inv_line_items` tables; no DROP/TRUNCATE/data rewrites; safe to deploy with zero downtime | Local validation: `prisma generate`; `tsc --noEmit`; `npm run check:migrations`; adds credit-note, immutability, and ROT/RUT columns for native invoicing | Engineering lead |
| 2026-06-06 | `20260606000002_invoice_permissions` | Apply with the standard deployment backup procedure; migration is purely additive `INSERT ... ON CONFLICT DO NOTHING` — no DROP, TRUNCATE, ALTER, or data rewrites, safe to deploy with zero downtime and safe to re-apply | Local validation (branch `claude/m3-invoicing`): `npm run check:migrations`; seeds `invoices.read`/`invoices.write`/`invoices.send`/`invoices.delete` into `usr_permissions` and grants them via `usr_role_permissions` (read to admin/user/viewer, write+send to admin/user, delete to admin only); no schema changes (data-only) | Engineering lead |
| 2026-06-06 | `20260606000003_invoice_number_nullable` | Apply with the standard deployment backup procedure; relaxing-only `ALTER COLUMN ... DROP NOT NULL` on the (empty) `inv_invoices` table so draft invoices hold NULL and the gapless number is assigned at issue; no data rewritten | Local validation: `prisma generate`; `tsc --noEmit`; `npm run check:migrations` | Engineering lead |
| 2026-06-05 | `20260605000002_time_entry_permissions` | Apply with the standard deployment backup procedure; migration is purely additive `INSERT ... ON CONFLICT DO NOTHING` — no DROP, TRUNCATE, ALTER, or data rewrites, safe to deploy with zero downtime and safe to re-apply | Local validation (branch `claude/m2-time-tracking`): `npm run check:migrations`; seeds `time_entries.read`/`time_entries.write` into `usr_permissions` and grants them via `usr_role_permissions` (read to admin/user/viewer, write to admin/user; viewer read-only); no schema changes (data-only) | Engineering lead |

## Access Reviews

| Date                      | Scope                                                                 | Result                                                                                                                                                        | Owner        |
| ------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Open gap as of 2026-04-24 | Application admin, repository, VPS, database, and CI/CD secret access | Repo-backed log and checklist now exist in `ACCESS_REVIEW_LOG.md` and `ACCESS_REVIEW_CHECKLIST.md`, but no completed quarterly review record is committed yet | ISMS Manager |

## Backup Restore Tests

| Date                      | Backup ref                                            | Restore target                                                                                                                                                  | Result  | Owner            |
| ------------------------- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------------- |
| Open gap as of 2026-04-24 | Production backup references are external to the repo | Repo-backed log and playbook now exist in `RESTORE_TEST_LOG.md` and `RESTORE_TEST_PLAYBOOK.md`, but no completed quarterly restore-test record is committed yet | Pending | Engineering lead |

## Internal Audit And Management Review

| Date                      | Activity          | Findings                                                                                                                                                          | Corrective actions                                                 | Owner        |
| ------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ------------ |
| Open gap as of 2026-04-24 | Internal audit    | Repo-backed log and audit workflow now exist in `INTERNAL_AUDIT_LOG.md` and `INTERNAL_AUDIT_PLAYBOOK.md`, but no completed internal audit record is committed yet | Schedule independent review and record findings/corrective actions | ISMS Manager |
| Open gap as of 2026-04-24 | Management review | Repo-backed log and agenda now exist in `MANAGEMENT_REVIEW_LOG.md` and `MANAGEMENT_REVIEW_AGENDA.md`, but no completed management review record is committed yet  | Record management review decisions, owners, and follow-up actions  | Management   |

## Incident And Vulnerability Operations

| Date                      | Activity                | Result                                                                                                                                                       | Owner            |
| ------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| Open gap as of 2026-04-24 | Incident drill/response | Repo-backed log and drill workflow now exist in `INCIDENT_POSTMORTEM_LOG.md` and `INCIDENT_RESPONSE_DRILL_PLAYBOOK.md`, but no record is committed yet       | ISMS Manager     |
| Open gap as of 2026-04-24 | Vulnerability review    | Repo-backed log and review workflow now exist in `VULNERABILITY_REVIEW_LOG.md` and `VULNERABILITY_REVIEW_PLAYBOOK.md`, but no review record is committed yet | Engineering lead |

## Supplier Reviews

| Date                      | Scope                                              | Result                                                                                                                                                                | Owner        |
| ------------------------- | -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Open gap as of 2026-04-24 | In-scope SaaS, hosting, database, and AI suppliers | Repo-backed log and review workflow now exist in `SUPPLIER_REVIEW_LOG.md` and `SUPPLIER_REVIEW_PLAYBOOK.md`, but no completed supplier review record is committed yet | ISMS Manager |

## Awareness And Training

| Date                      | Scope                                            | Result                                                                                                                                                                     | Owner        |
| ------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Open gap as of 2026-04-24 | In-scope engineering and admin/support awareness | Repo-backed log and workflow now exist in `SECURITY_AWARENESS_LOG.md` and `SECURITY_AWARENESS_PLAYBOOK.md`, but no completed awareness or training record is committed yet | ISMS Manager |

## Asset Lifecycle

| Date                      | Scope                                                     | Result                                                                                                                                                                                                                    | Owner        |
| ------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Open gap as of 2026-04-24 | Offboarding asset return and secure disposal/reuse events | Repo-backed lifecycle log and baselines now exist in `ASSET_LIFECYCLE_LOG.md`, `OFFBOARDING_AND_ASSET_RETURN_STANDARD.md`, and `ASSET_DISPOSAL_AND_REUSE_STANDARD.md`, but no completed lifecycle record is committed yet | ISMS Manager |
