import {
  handleGetTemplate,
  handleUpdateTemplate,
  handleDeleteTemplate,
} from '@modules/supporting/offers';

export const GET    = handleGetTemplate;
export const PUT    = handleUpdateTemplate;
export const DELETE = handleDeleteTemplate;
